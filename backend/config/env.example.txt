# Server Configuration
PORT=3000
NODE_ENV=development

# Spacecat API Configuration
SPACECAT_API_BASE_URL=https://spacecat.experiencecloud.live/api/v1
SPACECAT_API_KEY=your-api-key-here

# Azure OpenAI Configuration (Optional - for LLM comparisons)
AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com
AZURE_OPENAI_KEY=your-azure-api-key
AZURE_API_VERSION=2024-02-01
AZURE_COMPLETION_DEPLOYMENT=gpt-4o

# Data Storage
DATA_DIR=../data

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Analysis Configuration
PUPPETEER_CONCURRENCY=3
ANALYSIS_TIMEOUT=30000
PUPPETEER_HEADLESS=true

# Performance Configuration (NEW)
PUPPETEER_POOL_SIZE=2              # Number of browser instances in pool (default: 2)
CACHE_TTL=300                       # Cache time-to-live in seconds (default: 300)
CACHE_CHECK_PERIOD=60              # Cache cleanup check period (default: 60)
CACHE_MAX_KEYS=1000                # Maximum cache keys (default: 1000)

# Security Configuration (NEW)
RATE_LIMIT_WINDOW_MS=900000        # Rate limit window in ms (default: 15 min)
RATE_LIMIT_MAX_REQUESTS=100        # Max requests per window (default: 100)

# Logging Configuration (NEW)
LOG_LEVEL=info                      # Log level: debug|info|warn|error (default: info)

# File Upload
MAX_FILE_SIZE=10485760              # 10MB for URL files
MAX_BRAND_PRESENCE_FILE_SIZE=52428800  # 50MB for brand presence files

# External Tools (Optional - defaults to bundled versions)
# LLM_TRACKER_PATH=../path/to/llm-presence-tracker

# Note: Citation tracking uses JavaScript implementation in the backend.
# For standalone Python CLI tools, see: https://github.com/sharmarpit1989/tokowaka-measurement-automation

